version: '3.9'

services:
  postgres:
    image: jegj/postgres_16_uuidv7
    container_name: linktly_pg_db
    restart: unless-stopped
    ports:
      - 5433:5432
    volumes:
      - dbdata:/data/db
    env_file:
      - ./configs/.env
volumes:
  dbdata:
